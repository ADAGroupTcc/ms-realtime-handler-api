basePath: /
host: ms-chatpicpay-websocket-handler-api.ms.qa
info:
  contact:
    name: 'chatpicpay'
    url: https://picpay.atlassian.net/wiki/spaces/ChatPicPay/overview
  description: "This api will handle websockets connections for chatcore"
  termsOfService: http://swagger.io/terms/
  title: ms-chatpicpay-websocket-handler-api
  version: "1.0"
paths:
  /health:
    get:
      consumes:
        - '*/*'
      description: get the status of server.
      produces:
        - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Show the status of server.
      tags:
        - root
swagger: "2.0"
